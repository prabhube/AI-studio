/**
 * ImageGeneratorForm Component — Placeholder.
 *
 * WHY this component exists:
 *   Form for generating a single image via Stable Diffusion.
 *   Fields: prompt, negative prompt, width, height,
 *           inference steps, guidance scale, seed.
 *
 * Implementation: Phase 4.
 */

export function ImageGeneratorForm() {
  return (
    <div className="glass-card p-8 text-center">
      <p className="text-white/40">ImageGeneratorForm — Phase 4</p>
    </div>
  );
}
