/**
 * Redirect /dashboard to the route group layout.
 * This file keeps the /dashboard URL working while the
 * actual page lives in (dashboard)/dashboard/page.tsx.
 */
export { default } from "@/app/(dashboard)/dashboard/page";
